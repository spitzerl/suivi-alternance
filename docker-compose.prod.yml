services:
  # BDD
  db:
    image: postgres:15-alpine
    container_name: prod_postgres_db
    restart: always
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - prod_postgres_data:/var/lib/postgresql/data
    networks:
      - prod_network

  # Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: prod_node_backend
    restart: always
    env_file: .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRY=${JWT_EXPIRY}
    depends_on:
      - db
    networks:
      - prod_network
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.alternance-api.rule=Host(`${DOMAIN_NAME}`) && PathPrefix(`/api`)"
      - "traefik.http.routers.alternance-api.entrypoints=websecure"
      - "traefik.http.routers.alternance-api.tls.certresolver=myresolver"
      - "traefik.http.routers.alternance-api.priority=100"
      - "traefik.http.services.alternance-api.loadbalancer.server.port=5000"
      - "traefik.docker.network=proxy"

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        - VITE_API_URL=${VITE_API_URL}
    container_name: prod_react_frontend
    restart: always
    depends_on:
      - backend
    networks:
      - prod_network
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.alternance-web.rule=Host(`${DOMAIN_NAME}`)"
      - "traefik.http.routers.alternance-web.entrypoints=websecure"
      - "traefik.http.routers.alternance-web.tls.certresolver=myresolver"
      - "traefik.http.routers.alternance-web.priority=1"
      - "traefik.http.services.alternance-web.loadbalancer.server.port=80"
      - "traefik.docker.network=proxy"

networks:
  prod_network:
    driver: bridge
  proxy:
    external: true

volumes:
  prod_postgres_data:
